import type { Config } from 'tailwindcss';

const config: Config = {
    theme: {
        extend: {
            colors: {
                text: '#1A1A1A',
                strokes: '#2B2B2B',
                button: '#F3701E',
                one: '#F4ECE4',
                two: '#E8D8C9',
            }
        }
    }
}
export default config;
